/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SourceDetailService } from "../sourceDetail.service";
import { SourceDetailCreateInput } from "./SourceDetailCreateInput";
import { SourceDetailWhereInput } from "./SourceDetailWhereInput";
import { SourceDetailWhereUniqueInput } from "./SourceDetailWhereUniqueInput";
import { SourceDetailFindManyArgs } from "./SourceDetailFindManyArgs";
import { SourceDetailUpdateInput } from "./SourceDetailUpdateInput";
import { SourceDetail } from "./SourceDetail";
@swagger.ApiBearerAuth()
export class SourceDetailControllerBase {
  constructor(
    protected readonly service: SourceDetailService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "SourceDetail",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: SourceDetail })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: SourceDetailCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SourceDetail> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "SourceDetail",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"SourceDetail"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: data,
      select: {
        abstract: true,
        author: true,
        createdAt: true,
        id: true,
        keyword: true,
        title: true,
        uniqueSourceKey: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "SourceDetail",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [SourceDetail] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(SourceDetailFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SourceDetail[]> {
    const args = plainToClass(SourceDetailFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "SourceDetail",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        abstract: true,
        author: true,
        createdAt: true,
        id: true,
        keyword: true,
        title: true,
        uniqueSourceKey: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "SourceDetail",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: SourceDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: SourceDetailWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SourceDetail | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "SourceDetail",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        abstract: true,
        author: true,
        createdAt: true,
        id: true,
        keyword: true,
        title: true,
        uniqueSourceKey: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "SourceDetail",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: SourceDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: SourceDetailWhereUniqueInput,
    @common.Body()
    data: SourceDetailUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SourceDetail | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "SourceDetail",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"SourceDetail"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          abstract: true,
          author: true,
          createdAt: true,
          id: true,
          keyword: true,
          title: true,
          uniqueSourceKey: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "SourceDetail",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: SourceDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: SourceDetailWhereUniqueInput
  ): Promise<SourceDetail | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          abstract: true,
          author: true,
          createdAt: true,
          id: true,
          keyword: true,
          title: true,
          uniqueSourceKey: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
