/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ResourceSuggestionService } from "../resourceSuggestion.service";
import { ResourceSuggestionCreateInput } from "./ResourceSuggestionCreateInput";
import { ResourceSuggestionWhereInput } from "./ResourceSuggestionWhereInput";
import { ResourceSuggestionWhereUniqueInput } from "./ResourceSuggestionWhereUniqueInput";
import { ResourceSuggestionFindManyArgs } from "./ResourceSuggestionFindManyArgs";
import { ResourceSuggestionUpdateInput } from "./ResourceSuggestionUpdateInput";
import { ResourceSuggestion } from "./ResourceSuggestion";
@swagger.ApiBearerAuth()
export class ResourceSuggestionControllerBase {
  constructor(
    protected readonly service: ResourceSuggestionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "ResourceSuggestion",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: ResourceSuggestion })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: ResourceSuggestionCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ResourceSuggestion> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "ResourceSuggestion",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"ResourceSuggestion"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        ResourceID: {
          connect: data.ResourceID,
        },

        userID: {
          connect: data.userID,
        },
      },
      select: {
        createdAt: true,
        id: true,

        ResourceID: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        userID: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "ResourceSuggestion",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [ResourceSuggestion] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(ResourceSuggestionFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ResourceSuggestion[]> {
    const args = plainToClass(ResourceSuggestionFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "ResourceSuggestion",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        id: true,

        ResourceID: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        userID: {
          select: {
            id: true,
          },
        },
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "ResourceSuggestion",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: ResourceSuggestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: ResourceSuggestionWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ResourceSuggestion | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "ResourceSuggestion",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        id: true,

        ResourceID: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        userID: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "ResourceSuggestion",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: ResourceSuggestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: ResourceSuggestionWhereUniqueInput,
    @common.Body()
    data: ResourceSuggestionUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ResourceSuggestion | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "ResourceSuggestion",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"ResourceSuggestion"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          ResourceID: {
            connect: data.ResourceID,
          },

          userID: {
            connect: data.userID,
          },
        },
        select: {
          createdAt: true,
          id: true,

          ResourceID: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          userID: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "ResourceSuggestion",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: ResourceSuggestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: ResourceSuggestionWhereUniqueInput
  ): Promise<ResourceSuggestion | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          id: true,

          ResourceID: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          userID: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
